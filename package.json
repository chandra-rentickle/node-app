{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "keywords": [],
  "author": "Chandra",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.1"
  },
  "scripts": {
    "start": "node index.js",
    "test": "echo \"No tests specified\" && exit 0",
    "build": "echo \"Build step not required for Express.js\" && exit 0",
    "deploy": "echo \"Deploy step not required for Express.js\" && exit 0",
    "netlify-build": "echo \"No build command for Netlify functions yet\" && exit 0"
  }
}
